create table roles
(
    id   bigint generated by default as identity
        constraint roles_pkey
            primary key,
    name varchar(255)
);

insert into roles (name)
values ('ADMIN'),('EMPLOYEE'),('PATIENT');
